<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Sequences for education.subscribe -->
        <record id="center_application_sequence" model="ir.sequence">
            <field name="name">Demande d'inscription pour les étudiants</field>
            <field name="code">center.subscribe</field>
            <field name="prefix">SA</field>
            <field name="padding">3</field>
            <field name="company_id" eval="False"/>
        </record>
        <!-- Sequences for education.student -->
        <record id="center_student_sequence" model="ir.sequence">
            <field name="name">Admission des étudiants</field>
            <field name="code">center.student</field>
            <field name="padding">3</field>
            <field name="company_id" eval="False"/>
        </record>

        <!-- Sequences for education.document -->
        <!-- <record id="education_document_sequence" model="ir.sequence">
            <field name="name">Student Documents</field>
            <field name="code">education.document</field>
            <field name="prefix">DOC/%(range_year)s/</field>
            <field eval="1" name="number_next"/>
            <field eval="1" name="number_increment"/>
            <field eval="True" name="use_date_range"/>
            <field name="padding">3</field>
            <field name="company_id" eval="False"/>
        </record> -->
        <!-- Sequences for Academic Year -->
        <!-- <record id="education_academic_year_sequence" model="ir.sequence">
            <field name="name">Academic Year</field>
            <field name="code">education.academic.year</field>
            <field name="padding">3</field>
            <field name="company_id" eval="False"/>
        </record> -->
    </data>
</odoo>
